<%= render :partial => 'admin/shared/configuration_menu' %>
<%= error_messages_for :event %>

<%= form_for(:event, :url => collection_url, :html => { :multipart => true }) do |f| %>
<fieldset>

  <%= render 'form' %>
  <%= render 'admin/shared/new_resource_links' %>

</fieldset>
<% end %>
